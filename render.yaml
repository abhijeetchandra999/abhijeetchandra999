services:
  - type: web
    name: stock-prediction-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd "OneDrive/ドキュメント/stock" && gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
